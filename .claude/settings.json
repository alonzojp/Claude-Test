{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && git diff --cached --quiet || git commit -m \"Auto-commit from Claude Code\" && git push"
          }
        ]
      }
    ]
  }
}
